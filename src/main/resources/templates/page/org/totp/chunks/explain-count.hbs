{{#if useDays}}
    On average {{numberFormat perDay}} times a <b>day</b>
{{else if useWeeks}}
    {{numberFormat perWeek}} every week, on average
{{else}}
    {{numberFormat perMonth}} times a month, over the year
{{/if}}