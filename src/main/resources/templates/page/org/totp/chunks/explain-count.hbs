{{#if useDays}}
    On average {{numberFormat perDay}} every <b>day</b>
{{else if useWeeks}}
    {{numberFormat perWeek}} every week, on average
{{else}}
    {{numberFormat perMonth}} a month, over the year
{{/if}}