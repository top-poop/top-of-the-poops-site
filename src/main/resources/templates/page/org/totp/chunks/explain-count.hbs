{{#if useDays}}
    On average {{numberFormat1 perDay}} times a <b>day</b>
{{else if useWeeks}}
    {{numberFormat1 perWeek}} every week, on average
{{else}}
    {{numberFormat1 perMonth}} times a month, over the year
{{/if}}